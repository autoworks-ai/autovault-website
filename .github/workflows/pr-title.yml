name: PR Title

on:
  pull_request_target:
    types:
      - opened
      - edited
      - reopened
      - synchronize

permissions:
  pull-requests: read

jobs:
  conventional-pr-title:
    name: Lint PR Title
    # No base-ref guard: every PR title becomes squash-merge input eventually,
    # including a stacked PR's once its base lands. `pull_request_target` has no
    # branches filter, so removing the guard is all that is needed here.
    runs-on: ubuntu-latest
    steps:
      - name: Validate conventional PR title
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
        run: |
          pattern='^(feat|fix|docs|chore|refactor|test|perf|build|ci|revert)(\([[:alnum:]_.-]+\))?!?: .+'

          if [[ "$PR_TITLE" =~ $pattern ]]; then
            echo "PR title is valid: $PR_TITLE"
            exit 0
          fi

          echo "PR title must use Conventional Commits because squash merges become release input."
          echo "Allowed prefixes: feat, fix, docs, chore, refactor, test, perf, build, ci, revert"
          echo "Examples:"
          echo "  ci: deploy Cloudflare Pages previews"
          echo "  fix(auth): guard hosted vault checkout"
          exit 1
