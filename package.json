{
  "name": "autovault-website",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "ci": "npm run typecheck && npm test && npm run pages:functions:build && npm run docs:build",
    "dev": "npm run docs:dev",
    "docs:dev": "vitepress dev --host 0.0.0.0",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview --host 0.0.0.0",
    "dev:bootstrap": "wrangler d1 migrations apply autovault-hosted --local",
    "migrate:local": "wrangler d1 migrations apply autovault-hosted --local",
    "migrate:remote": "wrangler d1 migrations apply autovault-hosted --remote",
    "dev:pages": "npm run docs:build && wrangler pages dev .vitepress/dist --port 8788 --live-reload --persist-to .wrangler/state",
    "dev:stripe": "node scripts/stripe-listen.mjs",
    "deploy:pages": "npm run docs:build && wrangler pages deploy .vitepress/dist --project-name autovault-website",
    "pages:functions:build": "wrangler pages functions build functions --outdir .wrangler/tmp/pages-functions",
    "pages:preview-config": "node scripts/write-pages-preview-config.mjs",
    "test": "vitest run",
    "typecheck": "vue-tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@vitejs/plugin-vue": "^6.0.8",
    "concurrently": "^10.0.0",
    "vitepress": "^1.6.4",
    "vitest": "^4.1.8",
    "vue": "^3.5.41",
    "vue-tsc": "^3.3.10",
    "wrangler": "^4.95.0"
  },
  "dependencies": {
    "@clerk/backend": "^3.16.12",
    "@clerk/vue": "^2.4.33",
    "yaml": "^2.9.0"
  },
  "overrides": {
    "@clerk/shared": {
      "js-cookie": "3.0.7"
    }
  }
}
